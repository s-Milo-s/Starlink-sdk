# Environment variables for Starlink SDK

# Required: Your Starlink API secret
STARLINK_API_SECRET=your_api_secret_here

# Optional: Environment name (production, staging, development, demo, local)
# Defaults to 'production' if not set
STARLINK_ENVIRONMENT=production

# Optional: Enable debug logging
STARLINK_DEBUG=false
